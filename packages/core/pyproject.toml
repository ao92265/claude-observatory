[project]
name = "observatory-core"
version = "0.1.0"
description = "Shared JSONL reader, schema, pricing, SQLite store"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
observatory = "observatory_core.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
